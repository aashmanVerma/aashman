/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'media.licdn.com', 'pbs.twimg.com', 'cdn.sanity.io'],
  }
};

export default nextConfig;
